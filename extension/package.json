{
  "name": "tokenmanager-extension",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "TokenManager browser extension — track token expiry, never store token values.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tslib": "^2.7.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "@types/chrome": "^0.0.280"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0"
  }
}
