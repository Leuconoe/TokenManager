# Copy to .env and fill in. The OAuth 2.0 "Web application" client ID from the
# SAME Google Cloud project as the Android app (so the Drive appDataFolder is
# shared). See docs/06-setup/google-drive-sync.guide.md §3-B.
VITE_GOOGLE_OAUTH_CLIENT_ID=
